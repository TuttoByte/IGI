# Скопируйте в .env и при необходимости измените значения.
# Файл .env не коммитьте в репозиторий.

# Секретный ключ Django (для dev можно оставить пустым — подставится дефолт из base.py)
DJANGO_SECRET_KEY=
